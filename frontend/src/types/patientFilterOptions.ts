/**
 * Patient filter options from backend
 * Contains available programs and service types for filtering
 */
export interface PatientFilterOptions {
  programs: string[];
  serviceTypes: string[];
}
